<h1><%= t('tasks.edit.title') %></h1>

<% if @task.errors.any? %>
  <div>
    <% @task.errors.full_messages.each do |message| %>
      <p><%= message %></p>
    <% end %>
  </div>
<% end %>

<%= form_with(model: @task, local: true) do |form| %>
  <div>
    <%= form.label :title, Task.human_attribute_name(:title) %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.label :content, Task.human_attribute_name(:content) %>
    <%= form.text_area :content %>
  </div>

  <div>
    <%= form.submit t('tasks.edit.update'), id: 'update-task' %>
  </div>
<% end %>

<%= link_to t('tasks.edit.back'), tasks_path, id: 'back' %>